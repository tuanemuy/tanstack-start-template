/**
 * Form constants shared between presentation components. The application
 * layer owns runtime input validation — see each usecase's local Zod schema.
 */
export const TODO_TITLE_MAX_LENGTH = 140;
