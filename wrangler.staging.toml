# Staging deploy config — mirrors wrangler.toml but with -staging suffix on
# every Cloudflare-resource name so staging and production never collide
# inside a single Cloudflare account.
#
# Deploy via `pnpm deploy:staging` / `pnpm deploy:staging:<role>` — the
# scripts pass `--config wrangler.staging.toml` so wrangler does not pick up
# the dev-only `wrangler.toml` next to it.
#
# === Before first deploy =================================================
#   1. Replace `APP_URL` in every `[vars]` block below with the public URL
#      of the staging deployment (https://...). Leaving the placeholder
#      breaks OG image / canonical URL generation in `buildHead()`.
#   2. Replace `database_id` (`00000000-...`) in every `[[d1_databases]]`
#      block with the real D1 id printed by
#      `wrangler d1 create tanstack-start-template-d1-staging`.
#   3. Pre-create the queues (wrangler deploy does not auto-create them):
#        wrangler queues create tanstack-start-template-events-staging
#        wrangler queues create tanstack-start-template-events-dlq-staging
#   4. Inject secrets per worker via
#        wrangler secret put <NAME> --config wrangler.staging.toml [--env <role>]
# =========================================================================
#
# Wrangler does NOT inherit `d1_databases` / `vars` from top level into
# named environments — every `[env.*]` block re-declares them. Keep
# `database_id`, queue names, and `APP_URL` in sync across every block.
name = "tanstack-start-template-staging"
main = "app/server.cloudflare.ts"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = "./dist/client"
binding = "ASSETS"

[vars]
# TODO(staging): replace with the staging public URL before first deploy.
APP_URL = "https://staging.example.com"

[[d1_databases]]
binding = "DB"
database_name = "tanstack-start-template-d1-staging"
# TODO(staging): replace with the real D1 id from `wrangler d1 create ...`.
database_id = "00000000-0000-0000-0000-000000000000"
migrations_dir = "app/core/adapters/d1/migrations"

[[services]]
binding = "RELAY"
service = "tanstack-start-template-staging-relay"


# === Relay (Service Binding fetch + safety-net cron) ====================
[env.relay]
name = "tanstack-start-template-staging-relay"
main = "app/worker/cloudflare/relay.ts"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

[env.relay.vars]
APP_URL = "https://staging.example.com"
OUTBOX_BATCH_SIZE = "100"
OUTBOX_LEASE_MS = "300000"   # 5 min — covers worst-case single-batch dispatch
OUTBOX_MAX_ATTEMPTS = "2"    # multiplied by [env.consumer] max_retries — keep low

[[env.relay.d1_databases]]
binding = "DB"
database_name = "tanstack-start-template-d1-staging"
database_id = "00000000-0000-0000-0000-000000000000"
migrations_dir = "app/core/adapters/d1/migrations"

[[env.relay.queues.producers]]
binding = "EVENTS_QUEUE"
queue = "tanstack-start-template-events-staging"

[env.relay.triggers]
crons = ["*/5 * * * *"]


# === Consumer ===========================================================
[env.consumer]
name = "tanstack-start-template-staging-consumer"
main = "app/worker/cloudflare/consumer.ts"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

[env.consumer.vars]
APP_URL = "https://staging.example.com"

[[env.consumer.d1_databases]]
binding = "DB"
database_name = "tanstack-start-template-d1-staging"
database_id = "00000000-0000-0000-0000-000000000000"
migrations_dir = "app/core/adapters/d1/migrations"

[[env.consumer.queues.consumers]]
queue = "tanstack-start-template-events-staging"
max_batch_size = 25
max_batch_timeout = 30 # seconds
max_retries = 3
dead_letter_queue = "tanstack-start-template-events-dlq-staging"


# === Pruner =============================================================
[env.pruner]
name = "tanstack-start-template-staging-pruner"
main = "app/worker/cloudflare/pruner.ts"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

[env.pruner.vars]
APP_URL = "https://staging.example.com"
OUTBOX_RETENTION_MS = "604800000"   # 7 days

[[env.pruner.d1_databases]]
binding = "DB"
database_name = "tanstack-start-template-d1-staging"
database_id = "00000000-0000-0000-0000-000000000000"
migrations_dir = "app/core/adapters/d1/migrations"

[env.pruner.triggers]
crons = ["0 3 * * *"] # 03:00 UTC daily


# === DLQ ================================================================
[env.dlq]
name = "tanstack-start-template-staging-dlq"
main = "app/worker/cloudflare/dlq.ts"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

[env.dlq.vars]
APP_URL = "https://staging.example.com"

[[env.dlq.d1_databases]]
binding = "DB"
database_name = "tanstack-start-template-d1-staging"
database_id = "00000000-0000-0000-0000-000000000000"
migrations_dir = "app/core/adapters/d1/migrations"

[[env.dlq.queues.consumers]]
queue = "tanstack-start-template-events-dlq-staging"
max_batch_size = 25
max_batch_timeout = 30 # seconds
max_retries = 1
